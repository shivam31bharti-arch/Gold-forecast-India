# PROMPT TEMPLATES

Store prompt templates for FinBERT sentiment tasks and Claude research queries. Example:

> "You are a finance-tuned sentiment model. Given this headline: <headline>, return polarity [-1,1], confidence, and topic tags."

Version and store templates to track prompt changes.
