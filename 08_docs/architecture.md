# ARCHITECTURE

Sketch:
[ingestion workers] -> raw -> staging -> feature pipeline -> training -> model registry -> deployment (api) -> monitoring

Notes on data lineage, persistence, and snapshot IDs.
